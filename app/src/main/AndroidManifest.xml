<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evsherpa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVSherpa"
        android:usesCleartextTraffic="true"
        android:name=".ui.login.CustomApplication"
        >

        <activity
            android:name=".ui.profile.AddressPage"
            android:label="@string/title_activity_address_page"
            android:theme="@style/Theme.EVSherpa.NoActionBar"></activity>
        <activity android:name=".ui.profile.PopUpUpdateAge" />
        <activity android:name=".ui.profile.AddressRegistration" />
        <activity android:name=".ui.profile.PopUpCarRegistration" />
        <activity android:name=".ui.profile.PopUpUpdateNickname" />
        <activity android:name=".PreferenceRegistrationActivity" />
        <activity android:name=".ui.setting.SettingActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".CarRegistrationActivity" />
        <activity android:name=".ui.signUp.SignUpActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".LoadingActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.EVSherpa.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.EVSherpa.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="b3bd3cd3b94f253b40ebeedb726de131"
            />
    </application>

</manifest>